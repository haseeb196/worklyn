-- Demo/seed data — run after creating at least one real auth user via
-- signup (Supabase Auth users can't be seeded via plain SQL insert).
-- Replace :user_id below with a real auth.users.id before running.

-- Example:
-- insert into clients (user_id, name, email, company, status) values
--   (:'user_id', 'Acme Co', 'hello@acme.test', 'Acme', 'active');
