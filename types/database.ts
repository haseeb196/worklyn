/**
 * Placeholder for Supabase generated database types.
 *
 * This file will be replaced in Phase 2 (Database & Security) by running:
 *
 *   npx supabase gen types typescript --project-id <project-id> --schema public > types/database.ts
 *
 * Until then, `Database` is intentionally loose so the Supabase clients
 * compile without a real schema.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
